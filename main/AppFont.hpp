#pragma once

class QApplication;

/**
 * Шрифт приложения.
 *
 * Без явного выбора Qt на Windows берёт «MS Shell Dlg 2» девятым кеглем —
 * шрифт диалогов Windows XP. Именно он, а не цвета, первым выдаёт возраст окна:
 * узкие буквы, тесные строки, всё на пол-кегля мельче, чем в любой программе
 * рядом. Segoe UI Variable — текст самой Windows 11; где его нет (Windows 10),
 * обычный Segoe UI. На маке система выбирает сама, и лучше неё не выберешь.
 *
 * Одна функция, чтобы окно и превью ставили один и тот же шрифт: снимок,
 * снятый другим шрифтом, показывает окно, которого у человека нет.
 */
namespace GreenRhythm {

    void applyAppFont(QApplication &app);

} // namespace GreenRhythm

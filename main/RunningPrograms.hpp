#pragma once

#include <QStringList>

namespace GreenRhythm {

    /**
     * Имена запущенных программ, как их увидит ядро.
     *
     * ТОЛЬКО ИМЯ, НИКОГДА ПУТЬ. Запрос пути к процессу, защищённому античитом, не
     * проходит, и программа, собранная «с путём», выпадает из выборки целиком —
     * ровно так игра однажды и не нашлась, а список выглядел исправным.
     *
     * К именам дописывается «.exe»: ядро сравнивает правила с именем файла, а
     * система такое имя отдаёт без расширения. Разойдись эти два написания — и
     * ни одно правило не совпало бы, оставаясь на вид настроенным.
     */
    QStringList runningPrograms();

} // namespace GreenRhythm

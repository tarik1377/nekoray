#pragma once

#include "Const.hpp"
#include "NekoGui_Utils.hpp"
#include "NekoGui_ConfigItem.hpp"
#include "NekoGui_DataStore.hpp"

// Switch core support

namespace NekoGui {
    inline int coreType = CoreType::SING_BOX;

    QString FindCoreAsset(const QString &name);

    QString FindNekoBoxCoreRealPath();

    bool IsAdmin();

    /**
     * Умеет ли эта платформа поднимать туннель ВНУТРИ процесса ядра.
     *
     * На Windows и Linux — да: там ядро можно запустить с нужными правами
     * (администратором либо с cap_net_admin), и оно откроет устройство само.
     *
     * На macOS — нет, и это не «пока не сделали». Ядро там запускается как
     * потомок интерфейса, интерфейс работает от обычного пользователя, а
     * повысить права уже запущенному процессу нельзя. Единственный путь —
     * отдельный привилегированный процесс (StartVPNProcess), и на маке он
     * используется всегда, независимо от настройки.
     *
     * Заведено ради конкретной поломки: на маке галка TUN отщёлкивалась назад
     * молча — ни диалога, ни строки в журнале. Ветка запроса прав знала только
     * про Linux и Windows, и на маке управление проваливалось прямиком в отказ.
     */
    bool PlatformSupportsInternalTun();

    /**
     * Поднимать ли туннель внутри ядра ПРЯМО СЕЙЧАС — настройка и платформа
     * вместе. Спрашивать надо это, а не голую настройку.
     *
     * Настройку человек когда-то включил, и она хранится; на маке она при этом
     * невыполнима. Голая настройка отвечала бы «да» и на маке — и конфигурация
     * получила бы вход типа tun, который непривилегированное ядро не откроет.
     * Это не «галка не сработала», это «подключение падает при старте», причём
     * с невнятной ошибкой изнутри sing-box.
     *
     * Само значение настройки при этом НЕ переписывается: человек мог включить
     * её на другой машине и синхронизировать настройки, и молча менять то, что
     * он задал, — худший способ решить вопрос платформы.
     */
    bool UseInternalTun();
} // namespace NekoGui

#define ROUTES_PREFIX_NAME QStringLiteral("routes_box")
#define ROUTES_PREFIX QString(ROUTES_PREFIX_NAME + "/")

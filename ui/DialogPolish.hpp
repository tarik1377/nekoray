#pragma once

class QWidget;

/**
 * Полировка диалога после setupUi.
 *
 * ЗАЧЕМ ОДНА ФУНКЦИЯ НА ВСЕ ДИАЛОГИ. Шестнадцать диалогов собраны в Qt Designer
 * в разные годы разными руками: у одних поля 9, у других 0; коробки QGroupBox с
 * рамкой и заголовком в разрыве линии; кнопки «OK» и «Cancel» по-английски, потому
 * что перевод Qt не доехал до пакета. Переписывать шестнадцать .ui ради полей и
 * подписей — неделя, и через год они разъедутся снова. Один проход по дереву
 * после setupUi даёт всем диалогам один язык и живёт в одном месте.
 *
 * ЧТО ДЕЛАЕТ. Поля и промежутки у всех раскладок; высота полей ввода и списков;
 * заголовки коробок — капителью, как подписи разделов в главном окне; кнопкам
 * стандартного ряда — русские подписи и порядок «главная справа»; полям ввода —
 * подсказка вместо пустоты, где её нет. Логику не трогает: ни одного сигнала,
 * ни одного имени объекта.
 *
 * ЧЕГО НЕ ДЕЛАЕТ. Не переставляет виджеты и не меняет раскладку: где в .ui
 * кнопки стоят посреди пустого места, они там и останутся — это правится в .ui.
 */
namespace GreenRhythm {

    void polishDialog(QWidget *root);

} // namespace GreenRhythm
